name: Hello workflows
on:
  push:
    branches:
      - feat/ci-cd

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: _Step_ setup node version 14
        uses: actions/setup-node@v2
        with:
          node-version: 14
      - name: _Before_ checkout
        run: ls
      - name: Checkout
        uses: actions/checkout@v3
      - name: _Step_ check node version
        run: ls
